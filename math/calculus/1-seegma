#!/usr/bin/env python3
"""
Calculates the summation of a function from a starting point to an ending point.
"""

import numpy as np

def seegma(f, a, b):
    """
    Calculates the summation of a function f(n) from a starting limit a to an end limit b.

    Args:
        f: A function that takes one argument (n) and returns a number.
        a (int): The starting limit of the summation (inclusive).
        b (int): The ending limit of the summation (inclusive).
        
    Returns:
        float: The result of the summation.
    """
    
    # Check for invalid limits
    if not isinstance(a, int) or not isinstance(b, int) or b < a:
        return None

    # Use NumPy's arange to generate the sequence [a, a+1, ..., b]
    # And then sum the result of applying the function 'f' to each number in the sequence.
    # Note: arange(start, stop) excludes 'stop', so we use b + 1
    
    try:
        # Generate the indices (n)
        indices = np.arange(a, b + 1)
        
        # Apply the function f to all indices and sum the results
        sum_result = np.sum(f(indices))
        
        return sum_result
        
    except Exception:
        # Handle cases where the function f might not be valid for numpy operations
        # or other unexpected errors.
        return None
